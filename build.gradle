apply plugin: 'java'
apply plugin: 'antlr'

version = '1.1.0'

repositories {
	mavenCentral()
}

dependencies {
	testCompile 'junit:junit:4.8.2'

	compile 'org.antlr:antlr:3.5.1'

	antlr 'org.antlr:antlr:3.5.1'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}
